<Window x:Class="PasswordManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager"
        mc:Ignorable="d"
		Title="MainWindow"
		Height="500"
		Width="450"
		Background="White">
    <Grid>
		<StackPanel>
			<Frame 
				   NavigationUIVisibility="Hidden"
				   x:Name="frame"></Frame>
			<TextBlock FontSize="28"
					   Margin="0 60 0 0"
					   HorizontalAlignment="Center">Hi, what would you like to do?</TextBlock>
			<Button FontSize="25"
					Margin="0 75 0 0"
					Height="45"
					Width="250"
					Click="Button_Click">Add Password</Button>
			<Button FontSize="25"
					Margin="0 50 0 0"
					Height="45"
					Width="250"
					Click="Button_Click_1">Get Password</Button>
			<Button FontSize="25"
					Margin="0 50 0 0"
					Height="45"
					Width="250"
					Click="Button_Click_2">Remove Password</Button>
		</StackPanel>
	</Grid>
</Window>
